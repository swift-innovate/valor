VALOR_PORT=3200
VALOR_DB_PATH=./data/valor.db
LOG_LEVEL=info
VALOR_AGENT_KEY=
# Set to true only for local/test workflows that intentionally allow header auth without VALOR_AGENT_KEY.
VALOR_ALLOW_ROLE_HEADER_FALLBACK=false

# Database backend: sqlite (default, zero setup) or postgres (production/scale)
DB_BACKEND=sqlite
# DB_POSTGRES_URL=postgresql://user:pass@localhost:5432/valor
# DB_POSTGRES_URL=postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres

# Provider: Anthropic Claude API
ANTHROPIC_API_KEY=

# Provider: Ollama (local models - works with bare Ollama or any Ollama-compatible proxy)
# Any Ollama-compatible endpoint: bare Ollama, Herd, LiteLLM, or another proxy
OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_STATUS_URL=http://localhost:11434/api/tags

# MCP server (agent communication via Model Context Protocol)
# MCP is mounted at /mcp on the same port as the REST API.
# Agents authenticate with VALOR_AGENT_KEY during MCP initialize handshake.
# No additional configuration needed — MCP starts automatically.

# Telegram bot gateway (Director mobile interface)
# Get a bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=
# Chat ID for the Director's Telegram chat (use @userinfobot to find yours)
TELEGRAM_CHAT_ID=

# Comma-separated gate names to disable. Leave blank to keep all gates enabled.
DISABLED_GATES=


# ── Claude Code Operative Integration ──────────────────────────────
# These are used by the Stop hook (scripts/valor-checkin.ts) and the
# valor-operative skill to identify this Claude Code instance to VALOR.
# Set these when running Claude Code as a VALOR operative.
VALOR_CALLSIGN=gage
VALOR_AGENT_ID=
# VALOR_URL defaults to http://localhost:3200 if not set
# VALOR_AGENT_KEY is shared with the engine — same key as above

# ── Models ─────────────────────────────────────────────────────────────
# All engine-wide model policy lives here; per-agent overrides live in
# agents/<id>/agent.md (Model Preferences: Default/Complex/Fast).
# Format: [provider/]model — e.g. ollama/gemma3:12b, anthropic/claude-sonnet-4-5
# VALOR_DEFAULT_MODEL=ollama/gemma3:12b
# CLAUDE_DEFAULT_MODEL=claude-sonnet-4-5
# ANALYST_MODEL=qwen3:latest
# DIRECTOR_MODEL=gemma3:27b
# DIRECTOR_GEAR2_MODEL=nemotron-cascade-2:latest

# ── Subscription CLI providers ─────────────────────────────────────────
# Use locally installed, subscription-authenticated CLIs as inference
# backends (no API keys). "auto" (default) registers the provider only if
# the binary is found; "on" forces registration; "off" disables it.
# Route to them via model prefix: codex/gpt-5.2-codex, grok/grok-4 — or
# bare "codex" / "grok" to use each CLI's own default model.
# CODEX_CLI=auto
# CODEX_CLI_PATH=codex
# CODEX_MODELS=codex,gpt-5.2-codex,gpt-5.2,gpt-5.1-codex-max
# GROK_CLI=auto
# GROK_CLI_PATH=grok
# GROK_MODELS=grok,grok-build,grok-composer-2.5-fast,grok-4,grok-code
